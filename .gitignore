# Agent/runtime artifacts
.pi-subagents/

# Python/test caches
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/

# Build artifacts (wheels/sdists)
/dist/
/build/

# Local manual-testing artifacts
out/
oxi.wav
stems/
# Exported / rendered audio and MIDI dropped at the repo root during manual testing
/*.wav
/*.mp3
/*.ogg
/*.flac
/*.mid
/*.midi
